export class Recovery {
  constructor(public firstName: string, public lastName: string, public email: string,
  			  public age: number, public imagePath: string, public city: string, public state: string) {}
}